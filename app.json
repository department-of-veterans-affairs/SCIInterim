{
  "name":"SCIInterim",
  "scripts":{
    "postdeploy":"bundle exec rake db:migrate db:seed && bundle exec rake app:seed_fake"
   },
  "env":{
    "LANG":{
      "required":true
    },
    "RACK_ENV":{
      "required":true
    },
    "RAILS_ENV":{
      "required":true
    },
    "SECRET_KEY_BASE":{
      "required":true
    }
  },
  "addons":[
    "heroku-postgresql"
  ]
}
